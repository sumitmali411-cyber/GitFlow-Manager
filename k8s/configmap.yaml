apiVersion: v1
kind: ConfigMap
metadata:
  name: gitflow-manager-config
  namespace: gitflow-manager
  labels:
    app: gitflow-manager
data:
  APP_ENV: "production"
  APP_NAME: "GitFlow Manager"
  FRAMEWORK: "angular"
