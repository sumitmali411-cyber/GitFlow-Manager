apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitflow-manager

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

commonLabels:
  app: gitflow-manager
  managed-by: kustomize
