apiVersion: v1
kind: Service
metadata:
  name: gitflow-manager
  namespace: gitflow-manager
  labels:
    app: gitflow-manager
spec:
  type: ClusterIP
  selector:
    app: gitflow-manager
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
